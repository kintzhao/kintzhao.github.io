{
  "name": "my-knowledge-site",
  "version": "1.0.0",
  "description": "个人知识管理网站 — 基于 Quartz v4",
  "private": true,
  "scripts": {
    "start":   "npx quartz build --serve",
    "build":   "npx quartz build",
    "sync":    "npx quartz sync",
    "new":     "node scripts/new-note.mjs"
  },
  "dependencies": {
    "@quartz-garden/quartz": "^4.4.0"
  },
  "engines": {
    "node": ">=20"
  }
}
