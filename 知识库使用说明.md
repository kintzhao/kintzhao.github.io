# 知识库使用说明

> 适用于基于 Quartz v4 搭建的个人知识管理网站。所有操作均在本地完成，写完后 `git push` 自动发布。

---

## 目录

1. [目录结构说明](#1-目录结构说明)
2. [写新文章](#2-写新文章)
3. [修改文章内容](#3-修改文章内容)
4. [移动文章位置](#4-移动文章位置)
5. [删除文章](#5-删除文章)
6. [分类管理](#6-分类管理)
7. [标签管理](#7-标签管理)
8. [发布到线上](#8-发布到线上)
9. [常用命令速查](#9-常用命令速查)
10. [图片处理](#10-图片处理)
11. [常见问题处理](#11-常见问题处理)

---

## 1. 目录结构说明

所有内容都在 `content/` 目录下，结构如下：

```
content/
├── index.md          ← 网站首页（必须存在）
├── notes/            ← 技术笔记
│   ├── index.md      ← 该分类的介绍页
│   └── python-async.md
├── reading/          ← 读书笔记
│   ├── index.md
│   └── atomic-habits.md
├── projects/         ← 项目记录
│   └── index.md
├── daily/            ← 日常思考
│   └── index.md
└── tools/            ← 工具箱
    └── index.md
```

**核心规则：**
- 每个文件夹建议有一个 `index.md`，作为该分类的介绍页
- 文件名用英文和连字符，如 `my-note.md`（避免中文文件名）
- 文件夹可以无限嵌套，如 `notes/python/async.md`

---

## 2. 写新文章

### 方法一：命令行快速创建（推荐）

```bash
# 进入项目目录
cd ~/ai_doc/web/kintzhao.github.io

# 创建技术笔记
npm run new -- notes/docker-basics "Docker 基础入门"

# 创建读书笔记
npm run new -- reading/deep-work "深度工作 读书笔记"

# 创建多级目录文章
npm run new -- notes/python/asyncio "Python asyncio 详解"
```

命令会自动在对应目录生成带模板的 `.md` 文件。

### 方法二：手动创建文件

在 `content/` 下新建任意 `.md` 文件，文件开头必须有 Front Matter：

```markdown
---
title: 文章标题
date: 2024-03-15
tags:
  - 标签一
  - 标签二
draft: false
description: 文章简介（显示在列表页）
---

## 正文从这里开始

正文内容...
```

### Front Matter 字段说明

|| 字段 | 必填 | 说明 |
||------|------|------|
|| `title` | 是 | 文章标题，显示在页面顶部 |
|| `date` | 否 | 创建日期，格式 `YYYY-MM-DD` |
|| `tags` | 否 | 标签列表，用于分类检索 |
|| `draft` | 否 | `true` 表示草稿，不会发布到网站 |
|| `description` | 否 | 文章摘要，显示在搜索结果和列表页 |
|| `aliases` | 否 | 文章别名，如 `["旧标题", "另一个名字"]` |

### 草稿功能

不想发布的文章，在 Front Matter 加一行：

```yaml
draft: true
```

本地预览可以看到，`git push` 后线上不会出现。等准备好了改成 `false` 再发布。

### 双链写法

在文章中引用其他笔记：

```markdown
参考 [[notes/python-async]]           ← 完整路径
参考 [[python-async]]                 ← 短路径（文件名唯一时可用）
参考 [[python-async|Python异步笔记]]  ← 自定义显示文字
```

---

## 3. 修改文章内容

直接用任意编辑器打开对应的 `.md` 文件修改即可。

```bash
# 用 VS Code 打开整个知识库
code ~/ai_doc/web/kintzhao.github.io

# 或直接打开某篇文章
code content/notes/python-async.md
```

**修改标题：** 改 Front Matter 里的 `title` 字段，不要改文件名（改文件名会导致其他文章的双链失效）。

**修改文件名：** 如果必须改文件名，同时要全局搜索替换所有引用了旧文件名的双链。

```bash
# 全局搜索某个文件名的引用
grep -r "旧文件名" content/
```

---

## 4. 移动文章位置

移动文件就是把 `.md` 文件从一个目录移到另一个目录。

```bash
# 把笔记从 notes/ 移动到 notes/python/ 下
mv content/notes/python-async.md content/notes/python/python-async.md

# 把文章从一个分类移到另一个分类
mv content/notes/book-review.md content/reading/book-review.md
```

**移动后必须做：** 检查并更新其他文章中指向这篇文章的双链。

```bash
# 查找所有引用了这篇文章的地方
grep -r "python-async" content/
```

如果用的是短路径双链 `[[python-async]]`，移动后文件名没变则无需修改。
如果用的是完整路径 `[[notes/python-async]]`，移动后需要更新为新路径。

---

## 5. 删除文章

```bash
# 删除单篇文章
rm content/notes/old-note.md

# 删除某个分类下的所有文章（慎用）
rm -rf content/old-category/
```

删除后同样要检查有没有其他文章引用了被删文章的双链，否则会出现断链。

```bash
# 查找断链（搜索被删文件名）
grep -r "old-note" content/
```

---

## 6. 分类管理

分类就是 `content/` 下的文件夹，管理分类即管理文件夹。

### 新建分类

```bash
# 新建分类文件夹，并创建介绍页
mkdir -p content/新分类名
cat > content/新分类名/index.md << 'EOF'
---
title: 分类标题
---

# 分类标题

这个分类存放...（写一句介绍）
EOF
```

示例：新建"课程笔记"分类

```bash
mkdir -p content/courses
cat > content/courses/index.md << 'EOF'
---
title: 课程笔记
---

# 课程笔记

各类课程、教程的学习记录。
EOF
```

### 重命名分类

```bash
# 把文件夹 notes 改名为 tech-notes
mv content/notes content/tech-notes
```

**重命名后必须做：**

1. 更新首页 `content/index.md` 中指向该分类的链接
2. 全局搜索替换所有引用旧分类路径的双链

```bash
# 搜索所有引用旧路径的文章
grep -r "notes/" content/

# 批量替换（把所有 [[notes/ 替换为 [[tech-notes/）
sed -i 's/\[\[notes\//[[tech-notes\//g' content/**/*.md
```

### 删除分类

```bash
# 删除整个分类（包含里面所有文章）
rm -rf content/old-category/
```

> ⚠️ 删除前建议先把分类里的文章移到其他地方，或者做好备份。

### 新建子分类（多级目录）

```bash
# 在 notes 下新建 python 子分类
mkdir -p content/notes/python
cat > content/notes/python/index.md << 'EOF'
---
title: Python 笔记
---

# Python 笔记

Python 相关学习记录。
EOF
```

---

## 7. 标签管理

标签在每篇文章的 Front Matter 中定义，无需提前创建，用到即生效。

### 给文章加标签

```yaml
---
title: 我的文章
tags:
  - Python
  - 异步编程
  - 后端
---
```

### 查看所有标签

网站会自动在 `/tags/` 路径生成标签索引页，访问 `https://kintzhao.github.io/tags/` 可以看到所有标签及对应文章。

### 批量修改标签名

假设想把所有文章的 `Python` 标签改成 `python`：

```bash
# 全局替换标签名
sed -i 's/- Python/- python/g' content/**/*.md
```

### 删除某个标签

```bash
# 从所有文章中删除某个标签（删除整行）
sed -i '/^  - 废弃标签$/d' content/**/*.md
```

---

## 8. 发布到线上

本地确认没问题后，三步发布：

```bash
# 第一步：把所有改动加入暂存区
git add .

# 第二步：提交，-m 后面写本次改动的说明
git commit -m "新增：Docker 基础笔记"

# 第三步：推送到 GitHub，自动触发构建
git push
```

推送后约 1-2 分钟，访问 `https://kintzhao.github.io` 即可看到更新。

### 本地预览（推送前确认效果）

```bash
# 启动本地服务器，实时预览
npx quartz build --serve

# 浏览器打开
http://localhost:8080
```

修改文件后页面会自动刷新，确认效果满意再 push。

### 查看发布状态

打开 `https://github.com/kintzhao/kintzhao.github.io/actions`，可以看到每次 push 的构建状态：

- 🟡 黄色圆圈：正在构建
- ✅ 绿色勾：构建成功，网站已更新
- ❌ 红色叉：构建失败，点进去看错误信息

---

## 9. 常用命令速查

### 日常操作

```bash
# 进入项目目录
cd ~/ai_doc/web/kintzhao.github.io

# 本地预览
npx quartz build --serve

# 新建笔记（自动生成模板）
npm run new -- notes/文件名 "文章标题"
npm run new -- reading/文件名 "书名"
npm run new -- daily/2024-03-15 "日记标题"

# 发布
git add .
git commit -m "说明"
git push
```

### 文件操作

```bash
# 移动文章
mv content/旧路径/文件.md content/新路径/文件.md

# 删除文章
rm content/路径/文件.md

# 新建分类
mkdir -p content/分类名

# 删除分类
rm -rf content/分类名/

# 搜索内容
grep -r "关键词" content/

# 查看所有文章列表
find content/ -name "*.md" | sort
```

### Git 操作

```bash
# 查看改动了哪些文件
git status

# 查看具体改动内容
git diff

# 撤销未提交的改动（恢复到上次 commit 的状态）
git checkout -- content/某篇文章.md

# 查看提交历史
git log --oneline -10
```

---

## 10. 图片处理

### 存放位置

图片统一存放在 `public/` 目录下：

```
public/
├── images/
│   ├── notes/           # 技术笔记图片
│   ├── reading/         # 读书笔记图片
│   └── projects/        # 项目记录图片
└── attachments/         # 其他附件
```

**创建图片目录：**

```bash
mkdir -p public/images/{notes,reading,projects}
```

### 引用方式

在 Markdown 中使用绝对路径引用（以 `/` 开头）：

```markdown
![图片描述](/images/notes/screenshot.png)

![架构图](/images/notes/architecture.png)
```

### 图片处理建议

| 场景 | 处理方式 |
|------|----------|
| 截图/照片 | 压缩后放入 `public/images/` |
| 流程图/架构图 | 推荐使用 Mermaid 代码绘制 |
| 小图标 | 使用 Emoji 或内联 SVG |
| 外部图片 | 直接使用 URL，但可能加载较慢 |

### 使用 Mermaid 绘图

无需图片文件，直接在 Markdown 中绘制流程图：

```markdown
​```mermaid
graph LR
    A[开始] --> B{判断}
    B -->|是| C[执行]
    B -->|否| D[结束]
​```
```

更多 Mermaid 语法参考：https://mermaid.js.org/

---

## 11. 常见问题处理

### Q1: 本地预览正常，线上图片不显示？

**原因**：图片路径大小写敏感或路径错误

**解决**：
1. 检查图片路径是否正确（Linux 服务器区分大小写）
2. 确保使用 `/images/...` 开头的绝对路径
3. 确认图片已提交到 Git 仓库

```bash
# 检查图片是否已提交
git status public/images/

# 提交图片
git add public/images/
git commit -m "添加图片资源"
git push
```

### Q2: 如何批量修改文章中的链接？

```bash
# 把所有旧路径替换为新路径
sed -i 's|旧路径|新路径|g' content/**/*.md

# 示例：替换图片路径
sed -i 's|!\[\](images/|![](/images/|g' content/**/*.md

# 示例：替换双链路径
sed -i 's|\[\[notes/|\[\[tech-notes/|g' content/**/*.md
```

### Q3: 推送后网站没有更新？

**排查步骤**：
1. 检查 GitHub Actions 构建状态（仓库 → Actions）
2. 确认推送到的是 `main` 分支
3. 等待 1-2 分钟让部署完成

```bash
# 查看当前分支
git branch

# 查看远程状态
git remote -v

# 确认最新提交已推送
git log --oneline -3
```

### Q4: 如何撤销已推送的更改？

```bash
# 撤销最近一次提交（保留文件更改）
git reset --soft HEAD~1

# 撤销最近一次提交（丢弃文件更改）
git reset --hard HEAD~1

# 强制推送（谨慎使用！会覆盖远程历史）
git push --force
```

### Q5: 构建失败怎么办？

**查看错误日志**：
1. 打开 GitHub 仓库 → Actions 标签页
2. 点击失败的构建记录
3. 展开查看详细错误信息

**常见错误**：
| 错误信息 | 解决方法 |
|----------|----------|
| `npm install` 失败 | 检查 `package.json` 语法，删除 `node_modules` 重试 |
| 构建超时 | 文章过多正常现象，耐心等待即可 |
| 找不到文件 | 检查 `content/` 目录结构，确认文件存在 |
| 权限错误 | 检查 GitHub Actions 权限设置 |

### Q6: 如何查看所有双链引用？

```bash
# 搜索引用某文章的所有位置
grep -r "\[\[python-async" content/

# 列出所有双链及其出现次数
grep -rho '\[\[[^]]*\]\]' content/ | sort | uniq -c | sort -rn

# 查找断链（引用了不存在的文章）
grep -rho '\[\[[^]]*\]\]' content/ | sort -u
```

### Q7: 如何添加 RSS 订阅？

Quartz 默认支持 RSS，访问地址：
```
https://kintzhao.github.io/index.xml
```

确保 `quartz.config.ts` 中已启用：
```typescript
Plugin.ContentIndex({
  enableRSS: true,
})
```

### Q8: 如何修改网站主题颜色？

编辑 `quartz.config.ts` 中的 `theme.colors` 配置：

```typescript
theme: {
  colors: {
    lightMode: {
      light: "#faf8f1",      // 页面背景
      secondary: "#5b7fa6",  // 链接/强调色
      // ... 其他颜色
    },
    darkMode: {
      // 深色模式颜色
    },
  },
}
```

---

## 附：Markdown 常用语法速查

```markdown
# 一级标题
## 二级标题
### 三级标题

**粗体**  *斜体*  ~~删除线~~

- 无序列表
- 第二项

1. 有序列表
2. 第二项

[链接文字](https://example.com)
[[双链笔记]]

![图片描述](/images/example.png)

`行内代码`

​```python
# 代码块
print("hello")
​```

> 引用块

|| 表头1 | 表头2 |
||-------|-------|
|| 内容1 | 内容2 |

> [!tip] 提示块
> 这是一个提示

> [!warning] 警告块
> 这是一个警告

> [!note] 笔记块
> 这是一个笔记

> [!info] 信息块
> 这是一个信息
```

---

*文档版本：v1.1 · 适用于 Quartz v4.5+*
